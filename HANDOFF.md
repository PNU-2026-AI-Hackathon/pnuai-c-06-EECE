# 인수인계 — 지금 상태

> **다른 LLM/사람이 이 저장소에 처음 붙을 때 먼저 읽는 문서.**
> 마지막 갱신 2026-08-23. 갱신할 때는 **날짜를 같이 고친다** — 낡은 상태 문서는 없는 것보다 나쁘다.

---

## 0. 읽는 순서

1. **이 파일** — 지금 어디까지 왔고 무엇을 밟으면 안 되는지
2. **[`apps/api/CLAUDE.md`](./apps/api/CLAUDE.md)** — 헌법. **2절(절대 불변 원칙)과 4절(범위 밖)은
   협상 대상이 아니다.** 여기 어긋나는 코드는 되돌린다.
3. [`README.md`](./README.md) — 해커톤 최종보고서. 제품 설명·성과·회고
4. [`docs/API_CONTRACT.md`](./docs/API_CONTRACT.md) — 응답 계약
5. [`apps/web/CLAUDE.md`](./apps/web/CLAUDE.md) — 화면 규약

**헌법 2절 요약** (원문을 꼭 읽을 것)

| | |
|---|---|
| 2-1 | LLM 은 **읽기만** 한다. 판정은 코드가 한다 |
| 2-2 | **모르면 모른다고 한다.** 추측을 사실처럼 쓰지 않는다 |
| 2-3 | **오탐이 최우선 적.** 조용한 실패가 시끄러운 실패보다 비싸다 |
| 2-4 | **못 한 일을 숨기지 않는다.** 안 돌린 규칙도 응답에 실린다 |

---

## 1. 이 제품

**Prefab** — 보드를 발주하기 **전에**, 회로도(넷리스트)와 펌웨어를 **대조**해서 어긋난 곳을 찾는다.

회로도만 보는 도구(Traceformer, Flux)는 이미 있다. **코드까지 같이 보는 도구가 없다.**
"컴파일도 되고 업로드도 되는데 보드가 안 도는" 부류의 결함이 표적이다.

- 제7회 부산대 창의융합AI해커톤 **창업 트랙 C-06 · 팀 전전컴**
- 저장소: `PNU-2026-AI-Hackathon/pnuai-c-06-EECE`

---

## 2. 배포

| | 주소 | 비고 |
|---|---|---|
| 화면 | https://prefab-web.onrender.com | Render 정적 사이트, SPA rewrite |
| API | https://pnuai-c-06-eece-prefab.onrender.com | Render Docker, **무료 플랜** |

- **GitHub 자동 배포가 걸려 있다.** main 에 머지하면 20~60초 뒤 반영된다.
  (Manual Deploy 를 누를 필요 없다 — 8/23 에 한 번 잘못 안내했다.)
- 설정은 루트 [`render.yaml`](./render.yaml) 한 파일에 있다.
- **무료 플랜이라 유휴 시 잠든다.** 첫 요청이 수십 초 걸릴 수 있다.

---

## 3. 지금 열려 있는 것

| PR | 상태 |
|---|---|
| [#89](https://github.com/PNU-2026-AI-Hackathon/pnuai-c-06-EECE/pull/89) 액션 자가 시험 | **빨간불 — `PREFAB_API_KEY` 가 죽었다.** 코드 문제가 아니라 8/26 04:25 에 DB 가 비면서 키가 같이 지워진 것이다. 새 키를 시크릿에 넣으면 초록이 된다 |
| [#57](https://github.com/PNU-2026-AI-Hackathon/pnuai-c-06-EECE/pull/57) 드리프트 데모 | **일부러 빨간불. 절대 머지하지 말 것** — 드리프트 CI 가 PR 을 막는 것을 보여 주는 데모다 |

**CI 가 초록인 것을 확인하기 전에는 머지하지 않는다.** main 이 이것 때문에 두 번 깨졌다.

```bash
gh pr checks 64
```

---

## 4. 구현된 것

**규칙 14개** (`apps/api/src/prefab/rules/`). `기본`은 흔한 검사, `차별`은 코드+회로도 대조라 우리만 하는 것.

| ID | 티어 | 무엇을 잡나 | 필요한 입력 |
|---|---|---|---|
| R01 | 차별 | 코드가 이 칩에서 못 쓰는 핀을 사용 | netlist, firmware |
| R02 | 기본 | SPI 플래시 전용 핀에 배선 | netlist |
| R03 | 기본 | 스트래핑 핀이 전원·접지에 직결 | netlist |
| R04 | 기본 | 외부 부품 출력이 GPIO 최대 정격 초과 | netlist, bom |
| R05 | 차별 | 칩이 지원 안 하는 주변장치 조합 | netlist, firmware |
| R07 | 차별 | 코드가 쓰는 핀이 회로도에 미연결 | netlist, firmware |
| R08 | 차별 | 회로도에 연결됐는데 코드가 초기화 안 함 | netlist, firmware |
| R09 | 기본 | 부팅 시 출력 나오는 핀에 부하 | netlist |
| R10 | 차별 | 회로도 변경 후 코드 미추종 (드리프트) | netlist, firmware |
| R11 | 기본 | 네트명 전압과 소스 부품 전원 도메인 불일치 | netlist |
| R12 | 기본 | 상위 전원 도메인이 하위를 직결 | netlist |
| R14 | 차별 | 같은 이름의 핀 상수가 서로 다른 핀을 가리킴 | firmware |
| R15 | 차별 | MCU 출력 하이가 상대 부품 입력 문턱에 미달 | netlist, firmware |
| R16 | 차별 | 안전값 쓰기 전에 출력 전환 → 부팅마다 순간 구동 | firmware |

> R06·R13 은 없다. 결번이다 — 번호를 당기지 말 것. 문서·테스트가 ID 로 참조한다.

**그 밖에**

- **데이터시트 사실 파이프라인** (`datasheet/`) — LLM 이 PDF 에서 값을 뽑고, **코드가 인용문을
  원문과 대조해** 통과한 것만 저장한다. 사실은 판정을 FAIL→PASS 로도, PASS→FAIL 로도 바꾼다.
- **규칙 발견 루프** (`discover/`) — LLM 이 **후보**를 제안하고 코드가 인용을 검증한다.
  **판정이 아니다.** 후보는 심각도를 안 달고 화면에서도 점선 테두리로 구분된다.
  이 루프가 찾은 결함이 R16 이 됐다.
- **드리프트 CI** (`.github/workflows/drift.yml`) — 회로도가 바뀐 PR 에서 **HEAD 코드로**
  변경 전/후를 각각 돌려 비교한다.
- **인증** (`web/auth.py`) — 8/23 추가. scrypt · HttpOnly 쿠키 · 세션 토큰은 DB 에 해시로.
- **요청 제한** (`web/ratelimit.py`) — 주소별 이중 창 토큰 버킷.
- **요금 안내** (`/pricing`, `GET /api/v1/usage`) — 숫자를 손으로 안 적고 DB 에서 읽는다.

**부품 사실 DB 에 부품 4개** — ESP32-C6 · HLK-LD2410C · JQC-3FF-S-Z · TP4056.

---

## 5. 돌리는 법

```bash
cd apps/api && .venv/bin/python -m pytest        # 725 passed (8/23 기준)
cd apps/web && npx tsc --noEmit
```

```bash
cd apps/api && PREFAB_DB=/tmp/dev.db .venv/bin/python -m uvicorn web.app:app --port 8000
```

화면은 `.claude/launch.json` 의 `prefab-web-with-api` 설정으로 띄운다 (포트 5174).
**`launch.json` 은 gitignore 다** — 각자 자기 것을 만든다.

CI 는 `pytest -q` 만 돌린다 (파이썬 3.11). ruff 는 CI 에 없다.

---

## 6. 밟으면 조용히 깨지는 것들

**이 절이 이 문서에서 제일 중요하다.** 전부 실제로 한 번씩 밟은 것이다.

### 6-1. 검사 ID 를 짧게 만들지 말 것

`web/service.py` 의 `CHECK_ID_BYTES` 는 **16 이상이어야 한다.**
`GET /api/v1/checks/{id}` 에는 **인증이 없고**(주인 없는 검사는 링크로 공유하라고 만든 것),
결과에는 사용자의 실제 소스 코드 줄과 네트·부품 목록이 들어 있다.
3바이트였던 시절 **초당 100회면 47시간에 전수 조사가 끝났다.**
`test_검사_ID_는_추측할_수_없어야_한다` 가 지킨다.

### 6-2. 규칙 안에서 네트워크·LLM 을 부르지 말 것

규칙은 **순수 함수**다. 요금 안내 화면이 "검사가 부르는 AI 호출 0번"이라고 말하고 있고,
그게 사업 모델의 근거다. `tests/test_check_is_offline.py` 가 **소켓 생성을 막고** 검사를
통째로 돌려 이걸 지킨다.

*"사실이 없으면 그때 데이터시트를 읽어 오자"* 는 생각은 자연스럽고, 한 줄이면 되고,
**넣는 순간 검사당 원가가 생긴다.**

### 6-3. 로컬에서 로그인이 안 되면 코드를 의심하기 전에 주소를 볼 것

화면이 `localhost:5174` 인데 API 가 `127.0.0.1:8000` 이면 브라우저 기준 **다른 사이트**라
세션 쿠키가 안 실린다. **로그인 요청은 200 으로 성공하고 그다음 요청부터 로그아웃 상태가 된다.**
검사·리포트는 쿠키가 필요 없어 멀쩡히 돌아가고 **로그인만 조용히 안 된다.**

→ `VITE_API_BASE` 를 `http://localhost:8000` 으로 둔다.

### 6-4. CORS 는 이제 보안 경계다

인증이 없던 동안에는 아니었고, 코드 주석에도 그렇게 적혀 있었다. **세션 쿠키가 생기면서 바뀌었다.**
허용된 출처의 페이지는 사용자의 쿠키를 실어 API 를 부를 수 있다.
배포에서는 `ALLOW_DEV_ORIGINS=0` 으로 개발 주소를 닫는다 (`render.yaml`).

`ALLOWED_ORIGINS` 는 개발 주소를 **더한다. 대체하지 않는다** — 대체하게 만들었더니 배포 주소를
넣는 순간 로컬 개발이 통째로, 조용히 막혔다.

### 6-5. 무료 플랜에는 영구 디스크가 없다 → 계정이 재배포마다 사라진다

Render 문서: *"You can attach a persistent disk to a **paid** Render web service."*

환경변수로 "영구임"이라고 **적어 두지 말 것.** 그건 주장이지 사실이 아니다 — 적어 놓고
디스크를 안 붙이면 화면은 안전하다고 말하고 계정은 사라진다.

대신 `web/storage.py` 가 DB 옆에 표식을 남기고 **다음 기동 때 살아 있는지 잰다.**
처음 뜨면 `unknown` 이고 그건 **"안전"이 아니라 "모름"** 이다.
유료 플랜 + `disk:` 를 붙이면 두 번째 기동부터 저절로 `persistent` 가 되고 화면 경고가 사라진다.
방법은 `render.yaml` 주석에 적혀 있다.

**재배포만이 아니다 — 놀려 두기만 해도 지워진다 (8/26 측정).**
"머지를 안 하면 시연 때까지 버티지 않겠나" 를 확인하려고 실제로 재 봤다.

```
03:17  계정 생성(201)   boots=1  first_seen=8/25 17:54
        ─ 그 뒤 아무것도 안 건드림 ─
13:25  깨움             boots=1  first_seen=8/26 04:25   ← 표식이 새로 생겼다
        그 계정으로 로그인 → 401
```

`first_seen` 이 바뀌었다는 것은 **표식 파일이 사라진 채로 새로 떴다**는 뜻이다.
Render 대시보드도 같은 말을 배너로 하고 있다 —
*"Your free instance will spin down with inactivity."*

**시연 때 쓸 계정·API 키는 그날 아침에 만들고, 서버를 깨워 둔다.**

```bash
while true; do curl -s -o /dev/null https://pnuai-c-06-eece-prefab.onrender.com/; sleep 600; done
```

### 6-6. 업로드는 디스크에 닿으면 안 된다

`/privacy` 가 "디스크에 쓰지 않습니다"라고 말한다. starlette 기본값(`spool_max_size` 1MB)이면
**1~10MB 파일은 임시 파일로 디스크에 쓰인다.** `web/app.py` 에서 상한 위로 올려 뒀다.
`test_업로드는_디스크로_넘어가지_않는다` 가 `SpooledTemporaryFile.rollover` 를 가로채 확인한다.

**화면 문구를 코드보다 앞서 쓰지 말 것.** 이 항목이 그렇게 생긴 버그였다.

### 6-7. `field == 상수` 꼴을 조심할 것

`normalize_output_type` 이 `field == OUTPUT_TYPE` 로 쓰여 있었는데, `field` 가 그 모듈에
import 된 `dataclasses.field` 로 해석돼 **항상 False 였고 예외도 안 났다.**
정규화가 통째로 안 돌았는데 테스트가 없어서 몇 주를 몰랐다.

### 6-8. 저장소에 넣으면 안 되는 것

- **GPL 저장소에서 가져온 보드 파일** — 이 저장소는 MIT 다. **목록(TSV)만** 커밋한다.
- **포스터·제출물** (`창업-*.pdf`, `*.pptx`, `*.ai`) — 추적하지 않는다.
- `*.db`, `*.db.boot` — gitignore 에 있다.

### 6-9. 근거 문구 규약 (헌법 11절)

판정 문구는 **조사를 맞추고**(`text.py` 의 `eun`/`i` 등), **단정하지 않는다.**
모르면 "…라면" 으로 쓴다. R15 초안이 넷리스트가 출력이라고 말하는 핀을 "입력"이라고 단정했다가
고쳤다. `tests/test_josa_in_claims.py` 가 조사를 지킨다.

### 6-10. `parts/*.json` 의 사실에는 **쪽 번호와 원문 인용**이 있어야 한다

없으면 `datasheet/` 의 검증기가 떨어뜨린다. **지어내지 말 것.**
측정값을 넣을 때는 출처를 "측정"으로 명시한다 (`jqc-3ff-s-z.json` 참고).

---

## 7. 아직 안 된 것 · 모르는 것

- **`JQC-3FF-S-Z` 의 입력 하이 문턱(V_IH)을 모른다.** R15 가 우리 보드에서 `UNRESOLVED` 로 뜬다.
  실제 증상("LED 가 ON 은 되는데 OFF 가 안 된다")과 **같은 네트**다.
  **원인이라고 단정하지 않는다.** 측정하면 확정되거나 해제된다.
  → 남은 측정: **D5 를 릴레이 IN 에서 뗀 상태로 IN 전압을 잰다.**
  3.6V 초과면 R04 가 맞고, 이하면 내가 `io_level` 을 입력 핀에 과하게 적용한 것이라 규칙을 좁혀야 한다.
- **R08 이 XTAL·UART 등 주변장치 전용 핀을 안 본다.** 크리스털이 달린 픽스처가 없어 **일부러 미뤘다.**
  확인할 보드 없이 표를 채우면 그게 다음 오탐이다.
- **모듈 핀아웃 DB 에 보드가 1개**(XIAO ESP32-C6). IPC-D-356 으로 들어온 보드에만 남은 문제다.
- **부품 사실이 4개뿐.** 없는 부품은 `UNRESOLVED` 로 나가고 **무엇이 있으면 풀리는지**를 같이 적는다.
- **비밀번호 재설정·메일 인증이 없다.** 메일 보낼 수단이 없다. 있는 척하지 않는다.
  → **8/26, GitHub 로그인을 붙여 우회로를 만들었다** (`web/github.py` · [설정 순서](docs/GITHUB_LOGIN_SETUP.md)).
  GitHub 으로 들어오면 비밀번호를 우리가 안 가지므로 잊을 것도 없다. **실서비스에서 동작 확인됨.**
  비밀번호 가입은 그대로 남아 있고, 그 길에는 위 문장이 여전히 참이다.

  이 기능의 보안은 **계정을 잇는 순서**가 전부다 —
  ① `github_id` → ② **인증된** 이메일 → ③ 새로 만들기.
  ②에서 「인증된」을 빼면 아무나 남의 주소를 자기 GitHub 에 적어 두고 그 계정을 가져간다.
  `tests/test_github_oauth.py` 가 이걸 붙잡는다.
- **결제가 없다** (헌법 4절). `/pricing` 의 Pro·팀은 "준비 중"이라고 적혀 있다.

---

## 8. 남은 일정

| 언제 | 무엇 | 상태 |
|---|---|---|
| **8/26 09:00** | 시연영상 (swedu@pusan.ac.kr 메일) | **미착수** |
| 8/26 09:00 | 온라인 투표시스템 가입 + 프로젝트 소개 (aipms.pusan.ac.kr) | 미착수 |
| **8/27 12:00** | 발표자료 (10분 이내, 플라토 게시판) | **미착수** |
| 8/27 12:00 | 유튜브 링크 → 투표시스템 + README | README 에 자리만 있음 |
| 8/28 09:00 | README 최종보고서 마감 | 계속 갱신 중 |
| 8/28 | 최종발표 | |

**코드보다 제출물이 급하다.**

---

## 9. 팀

박강현(프론트·팀장) · 조우진 · 유동훈 · 한지양(하드웨어) · 권지효

**팀장에게 "형님"이라고 부르지 않는다.**
커밋·푸시는 **요청받았을 때만** 한다. PR 은 열되, 머지는 확인을 받는다.
