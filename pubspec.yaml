name: pnu_bapmukja
description: 부산대 학생식당·지역상권 통합 웨이팅 플랫폼 "PNU 밥묵자"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.0
  qr_flutter: ^4.1.0
  shared_preferences: ^2.3.2

  # ── 추후 연동 (지금 활성화하지 말 것) ──
  # supabase_flutter: ^2.8.0   # Supabase 연동 시
  # mobile_scanner: ^5.2.3     # 운영자 QR 체크인 데모 시

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
