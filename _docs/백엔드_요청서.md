# 백엔드 요청서

프론트(박강현) → 백엔드(조우진 · 유동훈). **2026.08.20 갱신.**

> 이전 판(8/19)은 "1순위 R02·R03·R09, 3순위 R01·R05, 4순위 F-1" 이라고 적혀 있었는데
> **그게 전부 들어왔습니다.** 규칙은 11/11 이 돌고, 그 뒤에 입력 형식이 하나 늘었습니다.
> 이번 판도 **코드를 직접 돌려서 숫자를 다시 쟀습니다.**

---

## 1. 지금 상태 (main `4d6f3e3` 실측, 8/20)

```
규칙       11 / 11 동작   전부
테스트     473개 통과     xfail 0개
부품 사실   3개 부품 · 15건   esp32-c6 · hlk-ld2410c · jqc-3ff-s-z
LLM 호출   동작 확인      원문 대조로 지어낸 인용 걸러냄
검증 측정   검출 15/15 · 오탐 0/13   케이스 24개 (합성 23 · 실측 1)
입력 형식   2종           IPC-D-356 · KiCad 회로도 넷리스트(kicadxml)
```

실측 보드(`esp32-c6-presence-smart-light`):

| 입력 | 발견 |
|---|---|
| 넷리스트만 | 치명 2 |
| ＋ 펌웨어 | 치명 4 · 경고 1 |
| ＋ BOM ＋ 사실 | 치명 2 · 경고 1 · **해제 2** |

> **이 보드는 이제 옛 보드입니다.** 하드웨어 담당이 mmWave 센서를 카메라(OV3660)로
> 바꾸면서 MCU 도 **ESP32-S3** 로 옮겼습니다(8/20). **픽스처는 그대로 둡니다** —
> 골든 테스트·측정 기준선·샘플 검사가 전부 그 위에 서 있습니다.
> 새 회로도가 오면 **픽스처가 하나 더 생기는 것**이지 이걸 덮어쓰는 게 아닙니다.

---

## 2. 이미 된 것 — **다시 하지 마세요**

이전 요청서에 "열림"으로 적혀 있던 것 중 **이미 들어온 것들**입니다.
착수 전에 이 표를 먼저 보세요.

| 이전 번호 | 항목 | 어디에 |
|---|---|---|
| A-1 | 저항 반대쪽 터미널 (풀업/풀다운 구분) | `graph.passive_role()` |
| A-2 | R12 방향 단정 제거 | `r12_cross_domain.py` |
| A-3 | `Evidence.firmware()` `line` 선택적 | `types.py` |
| A-4 | CORS 5173~5175 | `web/app.py` `DEV_PORTS` |
| **A-5** | **R11·R12 dedup** | `engine.dedupe()` — 아래 주의 3번 |
| A++1 | 패드 1개 네트 = 미연결 | `Netlist.is_dangling()` |
| A+2 | 전원 레일 팬아웃 반영 | `graph.is_power_rail()` |
| **C-1** | 핀에 `silk`·`gpio` 싣기 | `netlist/pinmap.py` + `docs/CHIPS.md` |
| **C-2** | 펌웨어 파서 | `firmware/arduino.py` |
| **C-3** | R07 · R08 | 실측 보드에서 3건 검출 |
| 2-5 | R08 에 펌웨어 근거 | 근거 종류 `['netlist','firmware']` 확인 |
| B-1 | BOM CSV 파서 | `bom.py` |
| **B-3** | **LLM 데이터시트 추출** | `datasheet/extract.py` — 아래 주의 1번 |
| B-4 | 부품 사실 DB | `datasheet/store.py` · `facts.py` |
| B-5 | R11·R12 UNRESOLVED 해제 경로 | `rules/_clearance.py` |
| **B-6** | **R04** | `rules/r04_input_overvoltage.py` |
| **E-2 · E-3** | **검증 데이터셋 · 측정기** | `tests/fixtures/injected/` · `measure.py` |
| **R01·R02·R03·R05·R09** | **규칙 5개** | 8/19 에 전부 들어왔습니다 — 11/11 |
| **R10** | **드리프트** | 이전 넷리스트를 **선택 입력**(`ctx.git`)으로 받습니다 |
| **F-1** | **GitHub Action 드리프트 검사** | `.github/workflows/drift.yml` · `diff.py` |
| **F-4** | **샘플 검사 시드** | `samples/check.sample.json` · `service.seed_sample()` |
| **A+1 · A++1 · A++2** | 전원 레일 · 미연결 · 네트명 절단 | `graph.py` · `d356.py` |
| **kicadxml** | 회로도 넷리스트 입력 | `netlist/kicadxml.py` · `netlist/detect.py` |
| **`Chip.usb`** | 내장 USB 핀 (R08 오탐 차단) | `chips/__init__.py` |
| **`mpn.py`** | 부품번호 출처 통합 (BOM · 회로도) | `mpn.py` |

**카탈로그에 차단 사유가 남은 규칙은 이제 없습니다.** 11개 전부 구현됐고
`test_catalog.py` 가 "구현됐는데 차단 사유가 남아 있는" 상태를 막습니다.

---

## 3. 주의할 것 — **모르고 건드리면 되돌려야 합니다**

### 3-1. LLM 출력은 코드가 원문과 대조합니다

`datasheet/extract.py` 는 모델이 댄 인용문이 **그 쪽 원문에 진짜 있는지** 확인하고,
없으면 그 항목을 버립니다. 실제로 두 번 걸렸습니다 — 지어낸 인용 1건, 그리고
**사람이 손으로 넣은 값을 LLM 이 반박한 것 1건**.

- 인용문 비교는 **공백을 전부 지우고** 합니다. PDF 글자 레이어가 공백을 잃어버립니다
  (화면 `A GPIO, IO level 3.3V` → 추출 `AGPIO,IO level 3.3V`).
  이걸 "공백 하나로 줄이기"로 되돌리면 **진짜 인용문이 지어낸 것으로 판정됩니다**
- 추출 결과는 **DB 가 아니라 파일로** 나옵니다. 사람이 보고 커밋할지 정합니다

### 3-2. `voh_max` 와 `io_level` 은 다릅니다

모듈 데이터시트는 Voh 규격(min/max)을 **잘 안 줍니다.** 대신 "IO level 3.3V" 라고 적습니다.
그걸 `voh_max` 에 적으면 **없는 규격을 있다고 말하는 것**입니다. 실제로 한 번 그렇게
적었다가 LLM 추출이 반박해서 항목을 나눴습니다. 규칙은 `voh_max` 를 먼저 보고 없으면
`io_level` 을 씁니다.

### 3-3. dedup 기준은 심각도가 아니라 **근거**입니다

`engine.dedupe()` 는 같은 네트에서 **새로운 근거를 하나도 못 내놓는** 발견만 합칩니다.

```
R11 + R12   둘 다 넷리스트 근거뿐   → 합친다
R12 + R08   R08 은 펌웨어 근거      → 둘 다 남는다
R12 + R04   R04 는 데이터시트 근거   → R12 가 R04 로 합쳐진다
```

**심각도로 고르면 안 됩니다.** 그렇게 만들었더니 차별 등급 R08 이 기본 등급 R12 에
먹혀 사라졌습니다 — 우리 차별점이 중복 정리에 지워졌습니다.

### 3-4. 보드와 칩은 다른 부품입니다

BOM 에는 `XIAO-ESP32C6`(보드), 데이터시트는 `ESP32-C6`(칩)으로 나옵니다.
사실 파일의 `applies_to_boards` 가 잇습니다. **핀 전기 특성만 물려받습니다**
(`facts.CHIP_INHERITED`) — `vcc_nominal` 은 뺐습니다. 보드에는 레귤레이터가 있어서
(XIAO 는 USB 5V → 칩 3.3V) 칩 전압을 보드 전압이라고 하면 **그 틀린 값으로
R11·R12 가 판정합니다.**

### 3-5. 계약의 `needs` 어휘는 아직 3개입니다

엔진 내부(`INPUT_NAMES`)는 `datasheet` 를 알지만, **카탈로그의 `needs` 에 쓰면
프론트가 모르는 값이 나갑니다.** R11·R12·R04 는 `datasheet` 를 **선택적으로** 봅니다 —
`needs` 에 안 적고 사실이 없으면 UNRESOLVED 로 둡니다. 이 방식을 따라 주세요.
정말 필요하면 **계약 → 프론트 타입 → 카탈로그** 순서입니다.

### 3-7. 넷리스트가 **두 형식**입니다. 규칙은 그걸 몰라야 합니다

`netlist/detect.py` 의 `parse_any()` 가 **내용의 첫 글자로** 파서를 고릅니다.
확장자를 안 믿습니다 — 계약이 허용하는 `.txt` 는 둘 다일 수 있고 사용자는 이름을 바꿉니다.

| 같은 보드에서 얻는 것 | IPC-D-356 | kicadxml |
|---|---|---|
| 부품번호 · 데이터시트 URL | 0% | **100%** |
| 핀 이름 | **0%** (번호만) | **62%** |

**형식마다 다른 성질은 `Netlist.NAME_IS_WIDTH_LIMITED` 로 가릅니다.**
회로도 넷리스트는 이름을 안 자르므로 14자 절단 경고를 내면 **없는 문제를 만듭니다** —
`Net-(U3-LNA_IN)` 은 잘린 게 아니라 원래 그 이름입니다. 안 가르고 붙였다가
멀쩡한 이름 21개를 "잘렸을 수 있다"고 말할 뻔했습니다.

회로도 넷리스트에는 **좌표가 없습니다.** 기하 기반 실크 복원과 좌표 클러스터링이
안 돕니다. 그 사실을 `parse_notes()` 로 내보냅니다 — 숨기지 마세요.

### 3-8. 부품번호는 **두 길**로 옵니다. `bom.mpn_of()` 를 직접 부르지 마세요

`mpn.part_numbers(netlist, bom)` 하나만 보면 됩니다. **BOM 이 먼저**고
(사람이 직접 적은 것이 도구가 채운 것보다 셉니다) 회로도가 빈자리를 채웁니다.
출처는 `PartNumber.source` 에 남습니다 — **BOM 이 틀리면 BOM 을, 회로도가 틀리면
심볼 필드를 고칩니다.** "부품번호 미상" 한 마디로는 그 둘이 구별되지 않습니다.

한 번 여기서 절반만 고친 적이 있습니다 — 조회는 되는데 `_clearance.ask()` 가
`bom.mpn_of()` 에 매여 있어서 **BOM 없이는 해제가 안 났습니다.** 같은 보드에
BOM 을 넣고 빼서 비교해야 잡힙니다.

### 3-9. 칩 표의 **빈 값은 두 가지 뜻**입니다

```
input_only = ()    "이 칩엔 그런 핀이 없다"        (S3 · C6)
usb        = ()    "이 칩엔 내장 USB 가 없다"      (구형 ESP32)
boot_output= ()    "출처를 못 찾았다"              ← 뜻이 다르다
```

**못 찾은 것을 없다고 적으면 규칙이 조용히 틀립니다.** 칩마다 주석으로 밝혀 뒀고
`tests/test_chips.py` 가 값을 출처와 대조합니다. 칩을 새로 넣을 때 그 파일이 요구하는
것은 하나입니다 — **출처에서 읽은 값을 그대로 적었는가.**

### 3-6. 규칙은 순수 함수입니다

`check()` 안에서 네트워크·LLM·파일 IO·시간·난수 금지 (헌법 2-1).
데이터시트 조회는 **러너가 이미 끝내서** `Context.datasheet` 에 넣어 줍니다.
규칙은 `rules/_clearance.py` 를 통해서만 사실을 봅니다.

---

## 4. 지금 할 일

**먼저 `git pull` 하세요.** 브랜치를 바꾸면 `git status` 를 먼저 보세요 —
8/18 에 낡은 인덱스로 백엔드가 통째로 지워질 뻔한 일이 두 번 있었습니다.

### 1순위 — 새 보드(ESP32-S3 + OV3660) 받을 준비

하드웨어 담당이 회로도를 그리는 중입니다. **엔진 쪽 준비는 끝났습니다** —
칩 표에 S3 가 있고, BOM 없이 회로도만으로 부품 식별·데이터시트 해제가 납니다.

받으면 할 일:
1. 넷리스트를 `tests/fixtures/` 에 **새 픽스처로** 추가 (옛 보드를 덮지 않습니다)
2. `EXPECTED.md` 를 손으로 써서 정답표를 고정
3. 발견이 0건이면 **왜 0건인지 파이프라인 문구로 확인** — "이상 없음"과 "못 봤음"은 다릅니다

> **하드웨어 담당이 "PCB 를 안 떠서 넷리스트가 없다"고 했는데 그 전제가 틀립니다.**
> `kicad-cli sch export netlist --format kicadxml -o board.xml board.kicad_sch` 는
> 회로도 파일 하나로 돕니다. PCB 가 필요 없고, 오히려 그쪽이 정보가 더 많습니다.

### 2순위 — 부품 사실 채우기

**3개뿐입니다** (`esp32-c6` · `hlk-ld2410c` · `jqc-3ff-s-z`).
새 보드의 부품(OV3660 · ESP32-S3 모듈)이 들어오면 그만큼 늘어야 합니다.

```bash
PYTHONPATH=src .venv/bin/python -m prefab --extract <pdf> --mpn OV3660 \
  --source-url <주소> > parts/ov3660.json
```

결과는 **DB 가 아니라 파일로** 나옵니다. 사람이 보고 커밋할지 정합니다.
서식과 규칙은 `apps/api/parts/README.md`.

### 3순위 — R08 의 나머지 주변장치 전용 핀

USB 는 막았습니다(`Chip.usb` · `PERIPHERAL_PIN_NAMES`). **XTAL · UART 는 아직입니다.**

다만 **지금 픽스처에 크리스털을 단 보드가 없습니다.** 없는 상태로 만들면
상상으로 규칙을 짜게 되고, 그게 USB 오탐이 생긴 방식과 똑같습니다.
**새 회로도에 크리스털이 있으면 그때 실제로 뜨는 걸 보고 고치세요.**

### 4순위 — 결함 주입 케이스 늘리기

24개(합성 23 · 실측 1)입니다. 규칙을 고칠 때마다 양성·음성을 같이 넣어 주세요.

```bash
python scripts/make_injected.py && PYTHONPATH=src .venv/bin/python -m prefab --measure
```

**음성 케이스가 오탐율의 전부입니다** — 겉모습이 비슷한데 정상인 보드를 짝으로 두세요.

---

## 5. 아직 열려 있는 것

| # | 항목 | 막히는 것 |
|---|---|---|
| E-1 | 외부 검증 | **닫았습니다 (8/19).** 아래 설명 |
| — | 부품 식별률 | kicadxml 이면 100%, IPC-D-356 이면 BOM 이 있어야 합니다 |
| — | R08 XTAL·UART | 위 3순위. 픽스처가 없어서 미룹니다 |
| — | 새 회로도 | 하드웨어 담당 작업 중. **이게 지금 유일한 병목입니다** |

### E-1 은 방법이 아니라 데이터가 없어서 닫았습니다

표본 81쌍의 커밋 **5,299건**을 훑고 후보를 코드가 diff 로 검증했습니다.
**핀 배정이 실제로 바뀐 커밋은 2건**이고, 그 2건도 회로도를 안 건드려서
"회로도 ↔ 코드 어긋남"의 사례가 아닙니다.

원인은 **생존 편향**입니다 — 깃허브에 올라온 보드는 **작동해서 올라온 보드**고,
고치는 과정은 책상 위에서 끝나 커밋으로 안 남습니다.

> **공개 저장소는 검증 데이터가 될 수 없습니다.** 필요한 건 "완성 전" 상태인데,
> 그게 남는 저장소가 아닙니다.

그래서 **지금 숫자는 여전히 회귀 방지**이지 외부 재현율이 아닙니다.
측정기가 그 한계를 보고서에 직접 출력합니다 — **발표에서 그 문장을 빼지 마세요.**
전체 경위는 `_docs/규모_실험.md` 에 있습니다.

---

## 6. 대기 중 (백엔드 몫 아님)

- ~~**K1 릴레이 `IN`↔`VCC` 저항 측정**~~ — **끝났습니다.** 하드웨어 담당이 실물을 재서
  `parts/jqc-3ff-s-z.json` 에 들어갔습니다. `source_tier: "measured"` —
  **데이터시트가 아니라 실측이 출처인 첫 건**입니다. 예상대로 모듈 보드의 IN 핀 회로는
  부품 데이터시트에 안 나왔고, 재는 쪽이 답이었습니다
- **새 회로도** (ESP32-S3 + OV3660) — 하드웨어 담당 작업 중. 지금 유일한 병목입니다
- **카메라 모듈 인터페이스 확인** — OV3660 은 병렬(DVP)이라 S3 의 LCD_CAM 으로 받습니다.
  실제 배선이 오면 R05 가 볼 자리입니다

---

## 7. 자주 쓰는 명령

```bash
cd apps/api
PYTHONPATH=src .venv/bin/python -m pytest -q                    # 473개
PYTHONPATH=src .venv/bin/python -m prefab --measure             # 검출율·오탐율
PYTHONPATH=src .venv/bin/python -m prefab tests/fixtures/esp32-c6-presence-smart-light.d356 \
  --firmware tests/fixtures/esp32-c6-presence-smart-light.firmware \
  --bom tests/fixtures/esp32-c6-presence-smart-light.bom.csv

python scripts/make_injected.py                                  # 결함 주입 데이터셋 재생성
PYTHONPATH=src .venv/bin/python -m prefab --facts-load parts/*.json

# 회로도 넷리스트로 검사 (PCB 없이 회로도 파일만으로 뽑힙니다)
kicad-cli sch export netlist --format kicadxml -o board.xml board.kicad_sch
PYTHONPATH=src .venv/bin/python -m prefab board.xml --firmware fw/
```

**샘플을 다시 뽑을 때는 `PREFAB_DB` 를 빈 파일로 두세요.** 로컬 사실 DB 가 섞이면
커밋된 파일만으로 재현이 안 되고 `test_samples.py` 가 계속 빨간불입니다.

```bash
PREFAB_DB=/tmp/empty.db PYTHONPATH=src .venv/bin/python -m prefab \
  tests/fixtures/esp32-c6-presence-smart-light.d356 \
  --bom  tests/fixtures/esp32-c6-presence-smart-light.bom.csv \
  --firmware tests/fixtures/esp32-c6-presence-smart-light.firmware \
  --json > src/prefab/samples/check.sample.json
```

**엔진 출력이 바뀌면 프론트 목 데이터도 다시 뽑아 주세요.** 안 그러면 화면이
옛 결과를 보여줍니다.

```bash
PYTHONPATH=src .venv/bin/python -m prefab <위와 같은 인자> --json > ../web/src/mocks/check.json
PYTHONPATH=src .venv/bin/python -m prefab --rules-json > ../web/src/mocks/rules.json
```

---

## 8. 어디를 읽나

| 알고 싶은 것 | 읽을 곳 |
|---|---|
| 규칙 개수의 진실 | `apps/api/src/prefab/catalog.py` (문서 아님) |
| 프로젝트 헌법 | `apps/api/CLAUDE.md` |
| 사실 파일 쓰는 법 | `apps/api/parts/README.md` |
| R7·R8 정답표 | `apps/api/tests/fixtures/*.EXPECTED.md` |
| 경고가 뜨면 안 되는 경우 | `apps/api/tests/fixtures/NEGATIVE_CASES.md` |
| 왜 그렇게 정했나 | `_docs/결정_기록.md` |
| 왜 E-1 을 닫았나 · 형식 실험 | `_docs/규모_실험.md` |
| 칩 핀 제약과 그 출처 | `docs/CHIPS.md` (코드 사본은 `chips/__init__.py`) |
