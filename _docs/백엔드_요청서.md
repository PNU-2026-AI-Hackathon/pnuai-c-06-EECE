# 백엔드 요청서 — 프론트(박강현) → 백엔드(조우진 · 유동훈)

작성 2026.08.17 · 화면은 목 데이터로 이미 완성돼 있습니다.
**`VITE_API_BASE`에 URL 하나만 넣으면 붙습니다.** 계약대로 오면 프론트 코드 수정은 0입니다.
(배포 URL이 아니라 `http://localhost:8000`이어도 똑같이 붙습니다.)

**목표 응답 예시를 만들어 뒀습니다** — [`docs/examples/check.target-with-firmware.json`](../docs/examples/check.target-with-firmware.json).
R7·R8이 구현된 뒤 응답이 어떤 모양이면 되는지 필드까지 채워놓은 파일입니다.
이 모양으로 오면 프론트 수정이 필요 없습니다. (실제 결과가 아니라 명세입니다 — 숫자를 인용하지 마세요.)

우선순위 순으로 적었습니다. **배포는 후순위입니다** — 프론트는 `VITE_API_BASE`가 비면
목 데이터로 돌기 때문에 배포를 기다리지 않습니다. 0번은 **로컬에서 붙일 때** 필요한 것들입니다.

---

## 0-완료. 로컬 통합 테스트 결과 (2026.08.18) — **전부 통과**

실제 백엔드를 띄워 프론트를 붙였습니다. **처음으로 목이 아닌 실제 흐름을 태웠습니다.**

```bash
cd apps/api && python -m uvicorn web.app:app --port 8000        # Python 3.11+ 필요
cd apps/web && VITE_API_BASE=http://127.0.0.1:8000 vite --port 5173
```

| 경로 | 결과 |
|---|---|
| `GET /healthz` | `{"status":"ok"}` |
| `GET /api/v1/rules` | 11개. 업로드 화면에 `R01 · R05 · R07 · R08 · R10` 실제로 표시됨 |
| 업로드 → 검사 → 리포트 | **UI 로 파일 붙여서 끝까지 성공.** 발견 3건, 타일 `2/11`, 파이프라인 7단계 |
| `POST` 확장자 불일치 | 415 · 서버 문구 그대로 표시 |
| `POST` 넷리스트 누락 | 422 · 서버 문구 그대로 표시 |
| `GET /checks/없는id` | 404 · 서버 문구 그대로 표시 |
| 동기 실행 | POST 가 바로 `done`. `/c/{id}` 첫 폴링에서 `/r/{id}` 로 넘어감. **화면 수정 불필요 확인** |

계약대로 다 왔습니다. 프론트에서 고친 건 없습니다.

### 하나만 — CORS 기본값에 5173 만 있습니다

`web/app.py` 의 `DEFAULT_ORIGINS` 가 `localhost:5173` · `127.0.0.1:5173` 뿐입니다.
그런데 **vite 는 5173 이 점유돼 있으면 자동으로 5174 로 올립니다.**
그러면 CORS 가 조용히 막히고, 화면은 이유를 모른 채 기능을 잃습니다.
(제가 실제로 이걸 밟았습니다 — 5174 로 붙였다가 `/rules` 만 막혔습니다.)

둘 중 하나면 됩니다.

- `DEFAULT_ORIGINS` 에 `5174` · `5175` 를 더한다 (개발용이라 위험 없음)
- 또는 README 에 `--strictPort` 를 쓰라고 못 박는다

프론트 쪽은 이제 **막히면 막혔다고 말합니다** — 아래 참고.

---

## ~~0. 로컬에서 프론트-백엔드를 붙일 때 필요한 것~~ — **전부 닫힘**

> 8/18 통합 테스트로 4개 다 확인했습니다. 아래는 기록용으로 남깁니다.

### 0-1. 로컬 실행 방법 한 줄

`uvicorn web.app:app --reload --port 8000`이면 되는지, 다른 준비(환경변수 · API 키)가
있는지만 알려주세요. 붙일 때는 `VITE_API_BASE=http://localhost:8000`을 씁니다.

배포 URL은 **나중에** 주셔도 됩니다. 그때 환경변수만 갈아끼우면 됩니다.

### 0-2. CORS — 지금 넣어주세요

```
허용 origin: http://localhost:5173, <배포 URL — 정해지면 추가>
허용 method: GET, POST, OPTIONS
```

`POST /api/v1/checks`는 `multipart/form-data`라 **프리플라이트(OPTIONS)가 먼저 날아갑니다.**
GET만 열어두면 로컬에서 붙일 때부터 업로드가 통째로 막힙니다.
배포는 미루더라도 이건 지금 넣는 게 쌉니다.

### 0-3. `status: "running"`일 때 `pipeline`을 채워주세요

진행 화면(`/c/{id}`)은 **1초마다 `GET /api/v1/checks/{id}`를 폴링해서 `pipeline` 배열만 그립니다.**

- `status: "running"`이면 → 지금까지 끝난 단계가 실려 있어야 합니다. 나머지는 아직 안 실려도 됩니다.
- `status: "done"`이면 → 프론트가 자동으로 `/r/{id}`로 넘어갑니다.

running인데 `pipeline`이 빈 배열이면 진행 화면이 빈 화면이 됩니다.
검사가 1초 만에 끝난다면 이 항목은 무시하셔도 됩니다 — **그 경우 그렇다고만 알려주세요.**
바로 리포트로 보내도록 화면을 바꾸겠습니다.

### 0-4. 오류 응답 형식 고정

프론트는 `error.message`를 **사용자에게 그대로 보여줍니다.** 따로 문구를 만들지 않습니다.

```json
{ "error": { "code": "NETLIST_REQUIRED", "message": "넷리스트 파일이 필요합니다." } }
```

계약에 있는 4개(`NETLIST_REQUIRED` · `NETLIST_PARSE_FAILED` · `FILE_TOO_LARGE` · `CHECK_NOT_FOUND`)
외에 새 코드가 생기면 알려주세요. 코드를 몰라도 메시지는 뜨지만, 화면에서 다르게 다뤄야 할 게 있을 수 있습니다.

---

## ~~1. 지금 화면이 "지어내고 있는" 값~~ — **전부 닫힘**

> `rules_total` · `created_at` UTC · `GET /rules` 다 왔습니다. 프론트가 반영 완료했습니다.
> 아래는 기록용입니다.

### 1-1. `GET /api/v1/rules` 언제 됩니까 — **프론트는 준비 끝났습니다**

원래 업로드 화면에 `"코드 대조 규칙 5개 실행 불가"`처럼 숫자가 박혀 있었습니다.
규칙이 늘면 즉시 거짓말이 되는 값이라 **지웠습니다.**

지금은 이렇게 동작합니다.

| 카탈로그 | 화면 |
|---|---|
| 있음 | `needs`를 세어서 `실행 못 하는 규칙 2개 — R11 · R12` / `아직 구현 전 5개 — R1 · R5 · R7 · R8 · R10` |
| 없음 | **개수를 말하지 않습니다.** 지어내지 않기 위해서입니다 |

계약대로 응답하는 임시 서버로 양쪽 경로를 확인해 뒀습니다.
**엔드포인트가 뜨는 순간 프론트 수정 없이 숫자가 붙습니다.**

- `implemented: false`인 규칙도 꼭 포함해 주세요. "아직 구현 전"으로 따로 표시합니다.
- 목 모드용 `rules.json`은 **안 주셔도 됩니다.** 없으면 개수를 안 쓰는 쪽으로 처리했습니다.

### 1-2. `summary`에 전체 규칙 수가 없습니다

리포트 요약 타일에 "실행한 규칙 2/12"를 표시하는데, 지금은 이렇게 계산합니다.

```ts
`${summary.rules_run}/${summary.rules_run + summary.rules_skipped}`
```

**이 합이 항상 규칙 카탈로그 전체 개수와 같습니까?**
"해당 없어서 애초에 후보에도 안 든 규칙"이 생기면 이 합이 전체보다 작아집니다.
그렇다면 `summary.rules_total`을 추가해 주세요. 같다면 지금 방식 그대로 두겠습니다.

### 1-3. `created_at` 시간대

리포트 헤더에 `created_at.slice(0, 10)`으로 **날짜만** 찍고 있습니다.
계약 예시가 `Z`(UTC)라서 그대로 두면 한국 시간 오전 9시 이전 검사는 하루 전 날짜로 보입니다.

- UTC로 유지 → 프론트에서 KST로 변환해 표시하겠습니다. **이쪽을 권합니다.**
- KST 문자열로 준다 → 그대로 찍겠습니다.

둘 중 뭔지만 알려주세요.

---

## 2. 계약 확장 제안 — **2-3 은 급합니다**

> 원래 "8/25 이후여도 된다"고 적었는데 그 판단이 바뀌었습니다.
> **2-3(핀에 `silk`·`gpio`)은 R07·R08 의 하드 블로커**이고, 5절 트랙 C 의 C-1 과 같은 항목입니다.
> 나머지(2-1 · 2-5)는 여전히 급하지 않습니다.

### 2-1. `netlist` 근거에 구조화된 연결 정보

발견 카드의 "회로도가 아는 것" 레인에 **핀-네트 연결을 도식으로 그리려고 합니다.**
지금은 `evidence.text`가 줄바꿈으로 이어진 문자열이라, 그리려면 프론트가 이 문자열을
다시 파싱해야 합니다. 그건 파서를 두 벌 만드는 것이고 계약 위반입니다. 그래서 **안 했습니다.**

제안 (기존 `text`는 그대로 두고 선택 필드로 추가):

```json
{
  "kind": "netlist",
  "text": "U2.OUT → PRESENCE_3V3\nU1.LP-G → PRESENCE_3V3",
  "nodes": [
    { "ref": "U2", "pin": "OUT",  "net": "PRESENCE_3V3", "role": "source" },
    { "ref": "U1", "pin": "LP-G", "net": "PRESENCE_3V3", "role": "sink" }
  ],
  "highlight": ["U2.OUT"]
}
```

- `role`은 `source` / `sink` / `passive` 정도면 충분합니다. 판정에 못 쓰겠으면 빼도 됩니다.
- `nodes`가 없으면 지금처럼 `text`를 그대로 보여줍니다. **없다고 화면이 깨지지 않습니다.**
- **지금 당장은 필요 없습니다.** R7·R8이 돌기 시작해서 코드-회로도 대조가 실제로 나올 때
  같이 넣는 게 맞다고 봅니다.

### 2-3. 넷리스트 핀에 실크 라벨·GPIO를 실어주세요 — **R7·R8의 선행 조건**

이걸 못 하면 R7·R8이 우리 보드에서 못 돕니다. 우선순위 2번에 뒀지만
**R7·R8을 만들기 시작하는 시점에는 이게 먼저 끝나 있어야 합니다.**

픽스처를 파다 확인한 사실입니다.

```
U1 레코드 25개 → 이름 종류 18개
LP-G × 3   (LP_GPIO0 / LP_GPIO1 / LP_GPIO2)
SDIO × 3   (SDIO_DATA1 / DATA2 / DATA3)
```

IPC-D-356이 핀 이름을 4자에서 자르기 때문에 **물리적으로 다른 핀이 같은 이름으로 뭉칩니다.**
그래서 지금 응답으로는 `_IN_ACTIVE_LOW`에 붙은 `U1.SDIO`가 D3인지 D4인지 D5인지 알 수 없습니다.

```jsonc
// 현재
{ "ref": "U1", "pin": "SDIO" }

// 필요
{ "ref": "U1", "pin": "SDIO", "silk": "D5", "gpio": 23 }
```

`parts[].pins`도 같은 문제입니다 — 25개 패드가 18개로 뭉쳐서 D3·D4·D5가 `SDIO` 하나로 보입니다.

**좌표로 풀립니다.** 헤더가 두 열이고 피치가 0.1 inch라서 Y 내림차순이 실크 순서입니다.
대조표를 [`docs/CHIPS.md`](../docs/CHIPS.md) 모듈 핀아웃 절에 채워뒀습니다
(Seeed 공식 문서 출처 포함, 하드웨어 담당 확인 대기).

### 2-4. 파싱 노트 좌표 오프셋 — **직접 확인했습니다. 파서가 맞습니다**

`_incoming/parse_d356.py` 를 읽어보니 정규식 `X([+-]\d{6})Y([+-]\d{6})` 를 씁니다.
제가 우려한 문제는 없습니다. `README.md` 의 `[41:49]` 서술이 "X 문자를 포함한 토큰"이라는
뜻이었고, 파서는 애초에 오프셋을 안 씁니다. **문서만 애매했습니다.**

이 항목은 닫습니다. README 문구는 프론트가 정리하겠습니다.

### 2-5. R8 발견에 펌웨어 근거를 하나 붙여주세요

R8은 "회로도에 연결됐는데 코드가 초기화 안 함"입니다. 즉 **코드를 읽었고 그 핀이 없다**는 판정입니다.
그런데 `evidence`에 `kind: "firmware"`가 하나도 없으면 화면이 그 사실을 말할 방법이 없습니다.

지금은 이렇게 처리했습니다.

| 상황 | 화면 |
|---|---|
| 펌웨어 미제출 | `모름` — "펌웨어를 올리면 이 줄이 채워집니다" |
| 펌웨어 제출 + firmware 근거 없음 | `근거 없음` — "펌웨어는 읽었지만, 이 판정은 코드를 근거로 쓰지 않았습니다" |

**틀린 말이 아니라 틀린 말입니다.** 목표 응답을 실제로 화면에 렌더해 보고 확인했습니다 —
R8 카드는 "코드에 D5가 한 번도 나오지 않습니다"라고 주장해 놓고, 바로 아래 코드 레인에서
"이 판정은 코드를 근거로 쓰지 않았습니다"라고 씁니다. **카드가 자기 판정을 부정합니다.**

처음에는 `line: 1`을 억지로 넣는 안을 적어뒀는데, 그건 **없는 줄 번호를 지어내는 것**이라
"숫자를 지어내지 않는다"와 정면으로 충돌합니다. 사용자가 그 줄을 열어봅니다.

**그래서 계약을 고쳤습니다** — `firmware` 근거의 `line`을 `number | null`로 넓혔습니다.
([`API_CONTRACT.md`](../docs/API_CONTRACT.md) "부재도 근거다" 절). 이 모양으로 주세요.

```jsonc
{
  "kind": "firmware",
  "file": "smart_shoe_cabinet_v1.ino",
  "line": null,                       // 가리킬 줄이 없다 = 부재가 근거다
  "snippet": "검사한 파일 1개 · 106줄 · 참조한 핀 3개 (D2 · D3 · D10)\nD5 는 어느 파일에도 나오지 않습니다.",
  "highlight": ["D5"]
}
```

- 프론트는 **이미 양쪽 다 처리합니다.** `line`이 있으면 `파일명 : 줄`, 없으면 파일명만 찍습니다.
- 넓히기만 한 변경이라 기존 응답은 그대로 유효합니다. 이견 있으면 말씀해 주세요 — 되돌립니다.
- 목표 응답 예시에도 반영해 뒀습니다: `docs/examples/check.target-with-firmware.json`의 R08.

### 2-6. R12 근거 문구가 풀다운을 "pull-up" 이라고 적습니다 — **실제 버그**

`_incoming/check.py` 를 실제로 돌려서 확인했습니다. 합성 픽스처를 하나 만들어 넣었습니다:
[`apps/api/tests/fixtures/synthetic-divider-vs-pulldown.d356`](../apps/api/tests/fixtures/synthetic-divider-vs-pulldown.d356)

**먼저 좋은 소식** — 권지효님 Plant Butler 의 `1kΩ + 2kΩ` 분압 토폴로지는
**지금 엔진이 이미 통과시킵니다.** 오탐 없습니다. 직렬 소자가 네트를 끊어서
교차 도메인 조건이 성립하지 않기 때문이고, 우연이 아니라 구조적입니다.

**버그는 문구 쪽입니다.** 저항이 `GND` 로 가는 풀다운인데도 이렇게 나옵니다.

```
[FAIL] R12  net: SIG_A
       U2 (5.0V) drives U1 (3.3V) directly; passives on net: R1 (pull-up, not series)
                                                                 ^^^^^^^ 풀다운입니다
```

원인:

```python
series = [r for r in refs if re.match(r"^(R|D)\d", r)]
... f"passives on net: {','.join(series)} (pull-up, not series)" if series else ...
```

네트에 `R*` 이 있으면 **반대쪽 터미널을 안 보고** 무조건 "pull-up" 이라고 씁니다.

| 저항 반대쪽 | 정체 | 보호되나 |
|---|---|---|
| 상위 전원 (`3V3` · `5V_BUS`) | 풀업 | ❌ |
| `GND` | 풀다운 | ❌ |
| 다른 신호 네트 | 직렬 | ✅ (이때는 네트가 끊겨 R12 후보에 안 옴) |

**판정(FAIL)은 맞습니다. 문구만 고치면 됩니다.** 결론은 풀업이든 풀다운이든 "보호 아님"으로 같습니다.

우리 실제 보드의 R3 는 `3V3` 로 가는 **진짜 풀업**이라 지금 문구가 우연히 맞습니다.
그래서 실제 보드만 돌려서는 이 버그가 안 보입니다. 합성 픽스처를 넣은 이유입니다.

> 근거가 틀리면 판정이 맞아도 신뢰를 잃습니다. 사용자가 "풀업이라고? 나 풀다운 넣었는데"
> 하는 순간 끝입니다. 화면은 이 문구를 **그대로** 사용자에게 보여줍니다.

### 2-2. `firmware` 근거의 `file` 경로 기준

`file: "src/main.cpp"`가 **업로드한 zip 내부의 상대 경로**로 이해하고 그대로 표시하고 있습니다.
맞습니까? 절대 경로나 임시 디렉터리 경로가 섞여 오면 화면에 그대로 노출됩니다.

---

## 3. 목 데이터 동기화

`apps/web/src/mocks/check.json`은 **실제 보드를 파서·규칙 엔진에 돌린 결과**입니다.
손으로 적은 숫자가 하나도 없고, 앞으로도 손대지 않겠습니다.

엔진 출력이 바뀌면 (규칙 추가 · dedup · 필드 변경)
**새 JSON 파일 하나만 던져주세요.** 프론트가 그대로 교체합니다.

```bash
python -m prefab tests/fixtures/esp32-c6-presence-smart-light.d356 --json > check.json
```

이런 식으로 뽑을 수 있으면 제일 좋습니다.

### 관련 — 알려진 문제 2건이 화면에 보이는 방식

- **R11·R12 중복 검출 ([#1](../../issues/1))** — 지금 목 데이터에 `PRESENCE_3V3`가 두 카드로 뜹니다.
  dedup 되면 발견 수가 3 → 2로 줄 뿐, 화면은 그대로 동작합니다. **기다리겠습니다.**
- **모듈 핀아웃 DB 부재 ([#2](../../issues/2))** — 차별 규칙(R1·R5·R7·R8) 전체의 선행 조건이고,
  화면에서는 발견 카드의 "코드가 아는 것" 레인이 계속 "모름"으로 남습니다.
  그 상태를 숨기지 않고 그대로 보여주고 있으니, 데모에서 불리하지 않습니다.

---

## 4. 알림 — 프론트가 이미 고친 것

`docs/API_CONTRACT.md` 맨 아래 **"디자인 토큰 (프론트 공유)"** 절을 새 토큰으로 갈아끼웠습니다.
(제도 도면 톤 → 토스 계열 톤. 색 이름이 `vellum`/`redpen` → `bg`/`crit` 등으로 바뀌었습니다.)

**백엔드 동작에는 영향이 없습니다.** 응답 필드는 하나도 안 바뀌었습니다.
계약 파일을 양쪽이 공유하기로 했으니 알려만 둡니다.

---

---

# 5. 앞으로의 백엔드 작업 — 전체 목록

**2026.08.18 기준. 남은 기간 10일.**
의존성 순서로 적었습니다. 각 항목에 **완료 판정 방법**을 붙였습니다 — "됐다"의 기준이 사람마다 다르면 안 되니까요.

프론트는 어느 항목이 먼저 와도 됩니다. 계약대로만 오면 화면 수정이 없습니다(8/18 통합 테스트로 확인).

## 트랙 A — 근거 정확성 (작고 독립적, 지금 가능)

> **판정은 다 맞습니다. 근거 문구만 틀렸습니다.**
> 그런데 화면은 이 문구를 사용자에게 **그대로** 보여줍니다.
> 사용자가 "풀업이라고? 나 풀다운 넣었는데" 하는 순간 도구 신뢰가 끝납니다.
> 규칙을 늘리는 것보다 이게 먼저라고 봅니다 (`CLAUDE.md` 2-3).

| # | 작업 | 완료 판정 |
|---|---|---|
| A-1 | `series_candidates()` 가 저항 **반대쪽 터미널**을 보게 한다. 상위 전원이면 풀업, `GND` 면 풀다운, 다른 신호면 직렬 | `python -m prefab tests/fixtures/synthetic-divider-vs-pulldown.d356` 의 `SIG_A` 가 "풀다운"이라고 나온다 |
| A-2 | R12 근거에서 **신호 방향 단정을 뺀다.** `K1 drives U1` 인데 K1 의 그 패드는 입력이다 | `_IN_ACTIVE_LOW` 근거가 "K1 이 구동한다"고 말하지 않는다 |
| A-3 | `Evidence.firmware()` 팩토리를 `line: int \| None = None` 으로 넓힌다. 필드는 이미 `int \| None` 인데 팩토리가 막고 있다 | R8 이 `line: null` 근거를 만들 수 있다 |
| A-4 | `DEFAULT_ORIGINS` 에 `5174` · `5175` 추가 (또는 README 에 `--strictPort` 명시) | 5174 로 띄운 프론트에서 `/rules` 가 뜬다 |
| A-5 | R11·R12 dedup | `test_no_duplicate_net_across_rules` 의 strict xfail 을 지운다 |

근거: [`NEGATIVE_CASES.md`](../apps/api/tests/fixtures/NEGATIVE_CASES.md) N-1b · [`apps/api/CLAUDE.md`](../apps/api/CLAUDE.md) 알려진 버그 절.

## 트랙 B — 데이터시트 축 (**AI 활용의 유일한 자리**)

> **팀에서 순서를 앞당기기로 했습니다.** 이유는 두 가지입니다.
>
> 1. 지금 이 리포에서 **LLM 호출이 0회**입니다. AI 창업캠프인데 "AI 어디 썼나요"에 답이 없습니다.
> 2. 처방(`suggestion`)이 지금 "부품번호를 주세요" 수준입니다. 데이터시트 사실이 없으면 그 이상을 못 씁니다.

| # | 작업 | 선행 | 완료 판정 |
|---|---|---|---|
| B-1 | BOM CSV 파서 — `refdes, mpn, manufacturer, value` | 없음 | BOM 을 올리면 `parts_identified` 가 0 이 아니게 된다 |
| B-2 | MPN → 데이터시트 PDF 확보 | B-1 | 부품 1개라도 PDF 를 가져온다 |
| B-3 | **LLM 로 PDF → 전기적 사실 추출.** JSON 스키마 강제, 근거(쪽·표) 필수 | B-2 | `Voh` 같은 값 하나가 출처와 함께 나온다 |
| B-4 | 부품 사실 DB (SQLite) | B-3 | 같은 MPN 두 번째 조회에 **LLM 호출 0회** |
| B-5 | R11·R12 의 `UNRESOLVED` 해제 경로 | B-4 | 우리 보드 R12 `PRESENCE_3V3` 가 `PASS` 또는 `FAIL` 로 확정된다 |
| B-6 | R04 (외부 부품 출력이 GPIO 입력 정격 초과) | B-4 | 규칙당 테스트 3종 통과 |

**최소 버전이 작습니다.** 부품 **하나**만 하면 됩니다 — K1(`TONGLING JQC-3FF-S-Z`)이나 mmWave 센서 하나로 값을 뽑아 지금 뜬 경고 하나가 해제되거나 확정되면 됩니다. **"토폴로지가 질문을 던지고 데이터시트가 답한다"가 화면에서 한 번 작동하는 것**이 규칙 5개 늘리는 것보다 발표에서 강합니다.

> 마침 K1 은 지금 하드웨어 담당의 `IN`↔`VCC` 저항 측정을 기다리는 건입니다.
> 데이터시트로 풀리면 **측정 없이 해제됩니다.** 그게 이 제품이 파는 것입니다.

### 지켜야 할 선 — LLM 은 결론을 내지 않습니다

```
LLM:     데이터시트 3쪽 표에서 Voh = 3.3V 를 읽어 JSON 으로 반환
결정적:  Voh <= 3.6 비교는 코드가 한다
DB:      값을 저장 → 다음 사용자에게는 LLM 호출 0회
```

`CLAUDE.md` 2-1 그대로입니다. **LLM 출력이 캐시되어 결정적 자산이 됩니다** — 쓸수록 LLM 의존이 줄어드는 구조이고, 발표에서 이걸 말할 수 있습니다.

## 트랙 C — 펌웨어 축 (차별점의 중심)

| # | 작업 | 선행 | 완료 판정 |
|---|---|---|---|
| C-1 | **넷리스트 응답에 `silk`·`gpio` 를 싣는다** | 없음 | `{"ref":"U1","pin":"SDIO","silk":"D5","gpio":23}` 가 나온다 |
| C-2 | 펌웨어 파서 — 참조 핀 추출 + 상수 추적 | 없음 | `smart_shoe_cabinet_v1.ino` 에서 D2·D3·D10 을 뽑는다 |
| C-3 | R07 · R08 | C-1, C-2 | [`EXPECTED.md`](../apps/api/tests/fixtures/esp32-c6-presence-smart-light.EXPECTED.md) 의 발견 3건이 그대로 나온다 |
| C-4 | R01 · R05 (칩 표 연동) | C-2 | 규칙당 테스트 3종 |
| C-5 | R10 (드리프트) — git 이력 필요. 계약에 4번째 입력 추가 논의 | C-3 | — |

**C-1 이 하드 블로커입니다.** 지금 응답으로는 `_IN_ACTIVE_LOW` 의 `U1.SDIO` 가 D3·D4·D5 중 무엇인지 알 수 없어 R07·R08 을 만들 수가 없습니다. **좌표로 풀립니다** — 대조표가 [`docs/CHIPS.md`](../docs/CHIPS.md) 모듈 핀아웃 절에 있고 하드웨어 담당 확정까지 받았습니다(D2 = mmWave OUT, D5 = 릴레이).

### C-2 에서 지금 정해야 하는 것 — 순수 정적 분석인가, AI 추출인가

팀원 코드는 쉬웠습니다.

```c
const int LED_PIN = D10;
pinMode(LED_PIN, OUTPUT);
```

실제 코드는 이렇습니다.

```c
const int pins[] = {2, 3, 10};
for (int i = 0; i < 3; i++) pinMode(pins[i], OUTPUT);
```

배열·조건부 컴파일·라이브러리 래퍼까지 가면 순수 정적 분석은 커버리지가 안 나옵니다. **핀 추출이 틀리면 R07·R08 이 통째로 오탐 생성기가 됩니다.**

AI 로 간다면 안전장치가 필요합니다 — AI 가 **핀 후보 + 근거 위치(파일:줄)** 를 내고, 그 위치에 실제로 그 문자열이 있는지 **결정적으로 확인**하고, 확신 없으면 `UNRESOLVED`.

**나중에 바꾸면 파서를 두 번 만듭니다. 착수 전에 정해 주세요.**

## 트랙 D — 회로도 단독 규칙 (의존 없음, 기본 등급)

| # | 규칙 | 필요한 것 |
|---|---|---|
| D-1 | R02 회로도가 SPI flash 핀에 연결 | 칩 표 |
| D-2 | R03 strapping 핀 부팅 상태 오류 | 칩 표 |
| D-3 | R09 부팅 시 출력 나오는 핀에 부하 | 칩 표 |

셋 다 넷리스트만으로 됩니다. **다만 기본 등급입니다** — Traceformer·Flux 가 이미 하는 범위입니다. 차별점이 아니므로 트랙 B·C 뒤로 두는 게 맞다고 봅니다.

## 트랙 E — 검증 (숫자를 만든다)

| # | 작업 | 담당 |
|---|---|---|
| E-1 | 데이터셋 A — 오픈소스 레포 커밋 라벨링 (재현율) | 수집은 팀장이 진행 |
| E-2 | 데이터셋 B — **자체 결함 주입** (오탐율). 규칙당 3케이스 | 백엔드 |
| E-3 | 측정 스크립트 — 라벨된 스냅샷에 Prefab 을 돌려 재현율·오탐율 산출 | 백엔드 |

**E-2 는 지금 시작할 수 있습니다.** 합성 픽스처 만드는 법은 이미 있습니다 — [`synthetic-divider-vs-pulldown.d356`](../apps/api/tests/fixtures/synthetic-divider-vs-pulldown.d356) 이 그 예시이고, 레코드 생성 방식도 그 안에 있습니다.

> 주의: 자체 주입으로는 **재현율을 주장할 수 없습니다.** 우리가 우리 규칙을 알고 결함을 심으면 우리가 잡을 수 있는 것만 심게 됩니다. 자기 시험지를 자기가 출제하는 겁니다. **오탐율만** 여기서 나옵니다.

## 트랙 F — 나중 (여유 되면)

| # | 작업 | 비고 |
|---|---|---|
| F-1 | GitHub Action (PR 코멘트) | `후보A` 문서는 "제품의 본체"라 하고 README 로드맵은 "여유되면"이라 합니다. **팀에서 정해야 합니다** |
| F-2 | 배포 (Railway/Render) | 설정은 이미 있습니다. 후순위로 합의됨 |
| F-3 | AI 처방 — `suggestion` 을 값·대안까지 쓰고, **그 처방을 규칙 엔진에 되돌려 검사** | 트랙 B 이후. 계약의 `suggestion` 이 문자열 하나라 확장 논의 필요 — **지금 넓히지 말고** 처방이 어떤 모양이 되는지 본 뒤에 고치는 게 맞습니다 |

---

## 프론트가 기다리는 것만 다시 (우선순위)

| 순위 | 항목 | 없으면 화면에서 |
|---|---|---|
| 1 | **C-1** `silk`·`gpio` | R07·R08 발견이 영원히 안 옴 = 차별점이 화면에 한 번도 안 나타남 |
| 2 | **A-1 · A-2** 근거 문구 | 사용자에게 틀린 사실이 그대로 노출됨 |
| 3 | **B-5** UNRESOLVED 해제 | "판정 보류"가 계속 남아 데모에서 미완성으로 보임 |
| 4 | **A-3** `line: null` | R8 카드가 "코드에 없다"를 근거로 못 보여줌 |
| 5 | 2-1 `netlist` 근거 `nodes` | 토폴로지 도식 (없어도 화면 안 깨짐) |


## 요약 체크리스트

### 닫힘 ✅

`0-1` 로컬 실행 · `0-2` CORS · `0-3` 동기 실행 · `0-4` 오류 코드 ·
`1-1` `GET /rules` · `1-2` `rules_total` · `1-3` UTC · `2-2` 경로 기준 · `2-4` 좌표 오프셋

8/18 통합 테스트로 전부 확인했습니다. 계약대로 왔고 프론트 수정은 없었습니다.

### 열림

| # | 항목 | 트랙 | 막히는 것 |
|---|---|---|---|
| **C-1** | 핀에 `silk`·`gpio` 싣기 | C | **R07·R08 전체** |
| **A-1** | 저항 반대쪽 터미널 보기 (풀업/풀다운) | A | 근거 신뢰도 |
| **A-2** | R12 방향 단정 제거 | A | 근거 신뢰도 |
| **A-3** | `Evidence.firmware()` `line` 넓히기 | A | R8 근거 |
| **A-4** | CORS 기본값에 5174·5175 | A | 팀원 로컬 연결 |
| **A-5** | R11·R12 dedup | A | 발견 중복 |
| **B-3** | LLM 데이터시트 추출 | B | **AI 활용 서사 전체** |
| 2-1 | `netlist` 근거 `nodes` | — | 토폴로지 도식 (선택) |
| 2-5 | R8 에 펌웨어 근거 첨부 | C | R8 카드 설명력 |
| 3 | 엔진 출력 바뀔 때 새 `check.json` | — | 목 데이터 신선도 |

**결정이 필요한 것 2개** — C-2 를 순수 정적 분석으로 갈지 AI 추출로 갈지(착수 전에), F-1 GitHub Action 이 본체인지 여유 항목인지.
