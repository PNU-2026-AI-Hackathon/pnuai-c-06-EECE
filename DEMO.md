# 시연 가이드

## 실행

```bash
cd ~/pnuai-c-06
npm install        # 최초 1회
npm run dev        # http://localhost:3000
```

`npm run build:data` 는 **기본 시연 데이터를 바꿀 때만** 돌린다. 평소엔 필요 없다.

## 시연 전 준비 (1분)

1. `npm run dev` 를 띄우고 `localhost:3000` 이 뜨는지 확인
2. **`data/samples/pub-transactions-sample.csv` 를 바탕화면에 복사해 둔다** — 2막에서 끌어다 놓을 파일
3. 브라우저 탭 두 개: `localhost:3000` 과 `localhost:3000/settings`

> ⚠ 업로드한 데이터는 **서버 메모리에만** 있다. `npm run dev` 를 재시작하면 기본 매장으로 돌아간다.
> 리허설 후 재시작해 두면 깨끗한 상태에서 시작할 수 있다.

## 두 개의 데이터

| | 기본 시연 데이터 | 업로드 시연 파일 |
|---|---|---|
| 파일 | `data/pub-sales-pnu-2025.csv` | `data/samples/pub-transactions-sample.csv` |
| 형태 | 일별 집계 (하루 한 줄) | 결제 내역 (결제 한 건이 한 줄, 28,061건) |
| 기준 시점 | 2025-10-19 (중간고사 직전) | 2026-02-28 (겨울방학 끝물) |
| 가능한 분석 | 4/6 | **6/6** |

둘 다 **같은 매출**이다. 두 번째는 첫 번째를 결제 단위로 펼친 것이라 일별 합계가 정확히 일치한다.
다만 **결제 시각은 지어낸 값**이므로, 물어보면 솔직히 말한다. 시각 정보가 있으면 무엇이 켜지는지 보여주는 용도다.

---

# 1막 — 기본 화면 (약 2분)

| 순서 | 화면 | 말할 것 |
|---|---|---|
| 1 | `/` 홈 | "지난주 674만원, 전주보다 2.1% 올랐습니다" |
| 2 | 홈 예측 | "**다음 주는 21~35% 떨어집니다.** 중간고사거든요" — 438~533만원, 중앙값 492만원 |
| 3 | 홈 근거 | "중간고사 −24%p, 지난주 특이 실적 −4%p, 지금까지 빗나간 방향 보정 +1%p" + **"비교한 사례는 중간고사 1번(5일)뿐이라 신뢰도 낮음"** — 표본이 적다는 걸 먼저 말한다 |
| 4 | 홈 검증 | "지난주 예측은 +7.5%, 실제 +13.9%. **6.4%p 틀렸습니다.** 화요일이 더 나왔고, 저희는 날씨를 아직 못 봅니다" |
| 5 | `/weekly` | 요일별(토 167만 > 금 133만 > 목 94만 > 일 51만), 메뉴 TOP(닭발 176만·26.2%) |
| 6 | `/weekly` 하단 | "**시간대별 매출은 비워뒀습니다.** 이 파일엔 결제 시각이 없어서요. 추측한 숫자는 안 보여드립니다" |

**3번과 4번과 6번이 이 제품의 차별점이다.** 표본이 적다고 먼저 말하고, 틀린 예측을 먼저 보여주고, 모르는 것은 비워 둔다.

---

# 2막 — 파일을 올리면 전부 바뀐다 (약 1분, 가장 중요)

6번에서 "결제 시각이 없어 못 합니다"라고 말한 직후에 바로 이어간다.

| 순서 | 동작 | 말할 것 |
|---|---|---|
| 7 | `/settings` 로 이동 | "그럼 시각이 있는 파일을 올려보겠습니다" |
| 8 | **샘플 파일을 끌어다 놓는다** | (읽는 동안) "사장님이 하시는 건 이것뿐입니다. POS에서 받은 파일을 끌어다 놓는 것" |
| 9 | 업로드 결과 | "**이 파일로는 6가지가 다 됩니다.** 아까는 4가지였고요. 무엇이 되고 무엇이 안 되는지 먼저 알려드립니다" |
| 10 | 홈으로 이동 | "**예측이 반대로 뒤집혔습니다. 다음 주 +19%.** 방학이 끝나가거든요. 화면이 중간고사에 맞춰 만들어진 게 아닙니다" |
| 11 | `/weekly` | "**아까 비어 있던 시간대별 매출이 채워졌습니다.** 21시가 60만원으로 가장 높습니다" |
| 12 | 홈 조기 종료 카드 | "하이볼이 평소 22:54까지 팔리는데 이날은 21:23이 마지막이었습니다. **3주 연속입니다.** 품절인지, 안 팔린 건지, 일찍 닫으신 건지는 저희가 모릅니다 — 사장님께 여쭙니다" |

10번이 이 시연의 정점이다. **미리 만들어 둔 화면이 아니라는 증거**이기 때문이다.

시연이 끝나면 `/settings` 의 **"예시 데이터로 되돌리기"** 로 원상복귀한다.

---

# 3막 — 정직함 (물어보면, 약 30초)

| URL | 보여줄 것 |
|---|---|
| `/?scenario=insufficient` | "데이터가 3주치면 **예측 자체를 안 합니다.** 대신 확실한 학사일정만 보여드립니다" |
| `/?scenario=stale` | "마지막 데이터가 6주 지나면 **예측을 멈춥니다.** 낡은 기준으로 계산한 숫자는 안 보여드립니다" |
| `/?scenario=mismatch` | "백엔드가 앞뒤 안 맞는 예측을 보내면 **화면이 잡아냅니다.** 근거 합계가 안 맞으면 숫자를 감춥니다" |

## 예측 정확도 (물어보면)

| 항목 | 값 |
|---|---|
| 예측 범위 (10/20~26, 중간고사 주) | **−35% ~ −21%** · 438~533만원 |
| 가장 가능성 높은 값 | −27% · 492만원 |
| 실제 (CSV 정답) | −24% · 512만원 |
| **범위 안에 들어왔는가** | **예** |
| 중앙값 기준 매출 오차 | 3.9% |
| 백테스트 검증 주 (10/06~12) | 예측 +7.5% / 실제 +13.9% (6.4%p 오차) |

모델: `일매출 = 기준 × 요일계수 × 학사이벤트계수 × 최근3주추세`

- 범위는 과거 주간 예측이 빗나간 폭(20~80% 분위)에서 만든다. **각 주의 오차는 그 주 시점의 모멘텀으로 잰다** (오늘의 추세를 과거에 소급하면 오차가 아니라 추세를 재게 된다)
- 모델이 한쪽으로 꾸준히 치우쳐 있으면 그만큼 예측값을 보정하고, 1%p 이상이면 **근거에 항목으로 드러낸다**
- 신뢰 수준은 기간 길이가 아니라 **비교 가능한 사례 수**로 정한다 (3회↑ 높음 / 2회 보통 / 1회 낮음)
- 검증은 대상 주 **이전 데이터만으로** 재학습해 비교한다 (미래 누출 금지)

학사이벤트 계수(실측) — 축제 1.67 / 기말종료직후 1.42 / 중간종료직후 1.37 / 개강초기 1.35 / 중간고사 0.68 / 기말고사 0.64 / 여름방학 0.31

## 예상 질문

**"학사일정은 어디서 가져오나요?"**
`data/academic-calendar.json` 에 저희가 갖고 있습니다. **실제 POS 파일에는 학사일정이 없습니다** — 그건 매장이 아니라 저희가 가진 정보입니다. 날짜만 보고 붙입니다. 시험이 끝난 다음 주는 "시험 종료 직후"로 자동 분류하고요.

**"아무 CSV나 됩니까?"**
날짜와 금액, 두 컬럼만 있으면 됩니다. 컬럼 이름은 POS마다 달라서 후보 목록으로 찾습니다. 메뉴가 있으면 메뉴 분석이, 결제 시각이 있으면 시간대별과 조기 종료가 추가로 켜집니다.

**"단가는 어떻게 아나요?"**
파일에 없어서 1년치 매출과 판매 수량으로 역산했습니다. **역산 오차 0.0000%** 입니다.

**"백엔드는요?"**
지금은 Next.js 서버가 분석합니다. 계산 로직은 `lib/analysis/` 에 순수 함수로 있어서 FastAPI로 그대로 옮깁니다. 프론트는 `lib/data.ts` 한 파일만 바꾸면 됩니다.

**"데이터는 저장되나요?"**
지금은 서버 메모리에만 있고 재시작하면 사라집니다. 파일 자체는 저장하지 않습니다.

## 지금 못 하는 것 (먼저 말할 것)

- **날씨** — 검증에서 틀린 이유를 설명하지 못하는 경우가 대부분 날씨다. 기상청 API로 붙일 예정
- **상권 전체와 비교** — "우리 가게 문제인가, 거리가 죽었나"에 아직 답 못 함. 부산대역 승하차 데이터로 붙일 예정
- **엑셀(.xlsx)** — CSV만 읽는다
- 실제 매장 데이터 0곳 — 지금 숫자는 전부 예시 CSV 계산 결과다

## 개발용 화면 (시연에는 쓰지 않음)

- `/preview` — 공통 컴포넌트 카탈로그
- `/dev/mocks` — 카페 목 데이터 시나리오
- `/?scenario=cafe` — 카페 예시 데이터
