# 시연 가이드

## 실행

```bash
cd ~/pnuai-c-06
npm install        # 최초 1회
npm run build:data # CSV → 화면 데이터 재생성 (CSV를 바꿨을 때만)
npm run dev        # http://localhost:3000
```

## 데이터

`data/pub-sales-pnu-2025.csv` — 부산대 앞 술집 1년치(2025-03-01 ~ 2026-02-28, 365일) 일별 매출.
`scripts/build-store-data.mjs` 가 이 CSV만 읽어 화면에 쓰는 모든 숫자를 계산한다. **손으로 적은 값 없음.**

시연 기준 시점은 **2025-10-19(일)**. 그 이후 데이터는 예측에 쓰지 않는다.

## 시연 동선 (약 3분)

| 순서 | 화면 | 말할 것 |
|---|---|---|
| 1 | `/` 홈 | "지난주 674만원, 전주보다 2.1% 올랐습니다" |
| 2 | 홈 예측 | "**다음 주는 22~32% 떨어집니다.** 중간고사거든요" — 예상 459~526만원 |
| 3 | 홈 근거 | "중간고사 −23%, 지난주 특이 실적 −4%. 시험 기간 하루 매출은 평소의 68%였습니다" + **"비교한 사례는 중간고사 1번(5일)뿐"** — 표본이 적다는 걸 먼저 말한다 |
| 4 | 홈 검증 | "지난주 예측은 +7.5%, 실제 +13.9%. **6.4%p 틀렸습니다.** 화요일이 19만원 더 나왔고, 저희는 날씨를 아직 못 봅니다" |
| 5 | `/weekly` | 요일별 매출(토 167만 > 금 133만 > 목 94만), 메뉴 TOP(닭발 176만·26%) |
| 6 | `/weekly` 하단 | "**시간대별 매출은 비워뒀습니다.** 이 파일엔 결제 시각이 없어서요. 추측한 숫자는 안 보여드립니다" |
| 7 | `/forecast?scenario=insufficient` | "데이터가 3주치면 예측 자체를 안 합니다" |

4번과 6번이 이 제품의 차별점이다. 틀린 예측을 먼저 보여주고, 모르는 것은 모른다고 말한다.

## 예측 정확도 (물어보면)

| 항목 | 값 |
|---|---|
| 예측 범위 (10/20~26, 중간고사 주) | **−32% ~ −22%** · 459~526만원 |
| 가장 가능성 높은 값 | −27% · 492만원 |
| 실제 (CSV 정답) | −24% · 512만원 |
| **범위 안에 들어왔는가** | **예** |
| 중앙값 기준 매출 오차 | 3.9% |
| 백테스트 검증 주 (10/06~12) | 예측 +7.5% / 실제 +13.9% (6.4%p 오차) |

모델: `일매출 = 기준 × 요일계수 × 학사이벤트계수 × 최근3주추세`.
범위는 과거 33주 예측이 빗나간 폭(20~80% 분위)에서 만들고, 같은 이벤트 표본이 10일 미만이면 그만큼 넓힙니다.
신뢰 수준은 기간 길이가 아니라 **비교 가능한 사례 수**로 정합니다 (3회 이상 높음 / 2회 보통 / 1회 낮음).
학사이벤트 계수(시연 시점까지 실측) — 축제 1.67 / 기말종료직후 1.42 / 중간종료직후 1.37 / 개강초기 1.35 / 중간고사 0.68 / 기말고사 0.64 / 여름방학 0.31.

## 시나리오 전환

| URL | 상태 |
|---|---|
| `/` | 술집 실데이터 (33주 학습) |
| `/?scenario=cafe` | 카페 예시 데이터 (시간대별 차트·놓친 기회 포함) |
| `/?scenario=insufficient` | 데이터 3주치 → 예측 불가 안내 |

시간대별 차트와 "3주 연속 품절" 화면을 보여주려면 `?scenario=cafe` 를 쓴다.

## 이 데이터로 못 하는 것 (먼저 말할 것)

- 결제 시각이 없어 **시간대별 매출 불가**
- 일별 집계라 **품절·놓친 기회 판단 불가**
- 메뉴 단가는 1년치 매출·수량에서 역산 (역산 오차 0.0000%)

## 개발용 화면 (시연에는 쓰지 않음)

- `/preview` — 공통 컴포넌트 카탈로그
- `/dev/mocks` — 카페 목 데이터 7개 시나리오

## 아직 없는 것

- 홍보 콘텐츠 화면, 설정(CSV 업로드) 화면 — "준비 중" 상태
- 백엔드(FastAPI) — 교체 지점은 `lib/data.ts` 한 파일
